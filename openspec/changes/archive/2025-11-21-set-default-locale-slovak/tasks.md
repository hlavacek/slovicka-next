## 1. Update Default Locale Configuration

- [x] 1.1 Change locale constant from `'en'` to `'sk'` in `i18n/request.ts`
- [x] 1.2 Update `<html lang>` attribute from `"en"` to `"sk"` in `app/layout.tsx`

## 2. Verification

- [x] 2.1 Run `npm run dev` and verify Slovak text appears in the UI
- [x] 2.2 Check browser DevTools that `<html lang="sk">` is rendered
- [x] 2.3 Run `npm run build` to ensure no build errors
- [x] 2.4 Verify all existing pages display Slovak translations correctly

## 3. Testing

- [x] 3.1 Manually test quiz setup page shows Slovak labels
- [x] 3.2 Manually test word set creation page shows Slovak labels
- [x] 3.3 Verify navigation and all UI elements use Slovak text
